= form_for @booking do |f|
  - if @booking.errors.any?
    #error_explanation
      %h2= "#{pluralize(@booking.errors.count, "error")} prohibited this booking from being saved:"
      %ul
        - @booking.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :Name
    = f.text_field :title

  .field
    = f.label :children
    = f.text_field :children
  .field
    = f.label :date
    = f.text_field :date
    -# = f.date_select :date
  .field
    = f.label :additionalRequirements
    = f.text_area :additionalRequirements
  .field

  -# add time to dropdown
  -#   = f.label :time
  -#   = f.collection_select :time_id, times.allm :id, :time, include_blank: true

  .actions
    = f.submit 'Save'
